spring:
  datasource: #数据库基本配置
    dynamic: #使用@DS切换数据源
      primary: master #设置默认的数据源或者数据源组,默认值即为master
      strict: false #严格匹配数据源,默认false. true未匹配到指定数据源时抛异常,false使用默认数据源
      datasource:
        master:
          url: jdbc:mysql://124.223.63.101:3306/ruoyi-vue-pro?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true
          username: root
          password: woshidashuaibi
          type: com.alibaba.druid.pool.DruidDataSource
        slave:
          url: jdbc:mysql://124.223.63.101:3306/ruoyi-vue-pro?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true
          username: root
          password: woshidashuaibi
          type: com.alibaba.druid.pool.DruidDataSource

--- #################### 阿里缓存操作框架相关配置 ####################
jetcache:
  areaInCacheName: false #设置不把areaName作为Key前缀
  local: # 本地缓存
    default:
      type: linkedhashmap
      keyConvertor: fastjson  # 使用json工具技术

  remote: # 远程缓存
    default: # 默认
      type: redis
      port: 6379
      host: 124.223.63.101
      valueEncoder: java
      valueDecoder: java
      keyConvertor: fastjson
      poolConfig:
        maxTotal: 50 #最大连接数
